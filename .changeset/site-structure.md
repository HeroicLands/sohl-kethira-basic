---
"sohl-kethira-basic": patch
---

The module's site is now its homepage at `/kethira/` and one page per note. Section listing pages, tag pages and `homepage-root` redirects are gone; index pages between are authoring responsibilities, declared as `doc` notes.
